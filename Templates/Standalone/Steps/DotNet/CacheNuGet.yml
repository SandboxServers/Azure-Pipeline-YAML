parameters:
  keySuffix: ''
steps:
- task: Cache@2
  displayName: 'Cache NuGet packages'
  inputs:
    key: 'nuget | "$(Agent.OS)" | $(Build.SourcesDirectory)/Directory.Packages.props | $(Build.SourcesDirectory)/global.json | ${{ parameters.keySuffix }}'
    restoreKeys: |
      nuget | "$(Agent.OS)"
    path: $(NUGET_PACKAGES)
